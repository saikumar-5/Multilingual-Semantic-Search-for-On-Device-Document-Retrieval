# Packaging

This folder is reserved for build scripts and release automation.

Planned additions:
- versioned build script for Windows (PyInstaller)
- model packaging with integrity checks
- build metadata manifest
